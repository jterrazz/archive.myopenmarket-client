import React, { useState } from 'react';
import PropTypes from 'prop-types';

const MetadataCase = ({ items }) => {
    return <div className='border border-gray'>test</div>;
};

MetadataCase.propTypes = {
    items: PropTypes.array,
};

export default MetadataCase;
